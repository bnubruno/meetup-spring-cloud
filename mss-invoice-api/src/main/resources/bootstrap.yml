spring:
  application:
    name: mss-invoice-api
server:
  port: 8081
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
feign:
  hystrix:
    enabled: true
